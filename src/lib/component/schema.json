{
  "$schema": "http://json-schema.org/schema",
  "$id": "SchematicsIMeanComponent",
  "title": "iMean Component Options Schema",
  "type": "object",
  "properties": {
    "name": {
      "type": "string",
      "description": "The name of the component.",
      "$default": {
        "$source": "argv",
        "index": 0
      },
      "x-prompt": "What name would you like to use for the component?"
    },
    "path": {
      "type": "string",
      "format": "path",
      "description": "The path to create the service."
    },
    "language": {
      "type": "string",
      "description": "Only supports ts."
    },
    "sourceRoot": {
      "type": "string",
      "description": "iMean service source root directory."
    },
    "type": {
      "type": "string",
      "description": "The type of the component.",
      "enum": ["component", "executor", "transformer"]
    }
  },
  "required": ["name"],
  "additionalProperties": false
}
